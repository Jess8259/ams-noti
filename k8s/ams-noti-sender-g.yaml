apiVersion: apps/v1
kind: Deployment
metadata:
 name: ams-noti-sender-g
spec:
 selector:
   matchLabels:
     app: ams-noti-sender-g
 template:
  metadata:
    labels: 
     app: ams-noti-sender-g
  spec:
   containers: 
    - name: ams-noti-sender-g
      image: ams-noti-sender-g
      ports:
        - containerPort: 8082
      resources:
        requests:
          cpu: 100m
          memory: 64Mi
        limits:
          cpu: 200m
          memory: 256Mi  
---
apiVersion: v1
kind: Service
metadata:
  name: ams-noti-sender-g
spec:
  type: ClusterIP
  selector:
    app: ams-noti-sender-g
  ports:
  - port: 8082